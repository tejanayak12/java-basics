100 == 100 // true (implict covert);

'100' === '100' // true (implict convert);

100 !== 5000 // true (implict conversation);
   
// (implict was an auto conversation which was done by java script);

// Implicit conversion to string
'25' + 15; // '2515'

// Implicit conversions to a number
23 * '2';   // 46
23 - true;  // 22  // true is converted into 1
true - null; // 1
false + undefined;  // NaN  // undefined into NaN

// conversion to string using String()
String(2 - true);  // '1'
'56' === String(56);  // true

String (99+1) // '100' true string;

// conversion to number using Number()
Number('23') + 7;  // 30;
Number (89+1) // 90;
 
Number (89+1) // 90;

